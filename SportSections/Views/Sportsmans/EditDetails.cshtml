@model SportSections.Models.SectionRecord

@{
    ViewData["Title"] = "Edit";
}

<link href="~/css/form.css" rel="stylesheet" />
<div class="form">
    <form asp-action="EditDetails">
        <div class="form__content">
            <div class="form__title">
                <span>Редагування</span>
            </div>
            <div class="form__items">
                <input type="hidden" asp-for="SectionRecordId" />
                <input type="hidden" asp-for="SportsmanId" />
                <div class="form__items__item">
                    <span class="display">Спортивна секція</span>
                    <select asp-for="SportSectionId" class="form-control" asp-items="ViewBag.SportSectionId"></select>
                    <span class="validation" asp-validation-for="SportSectionId"></span>
                </div>
                <div class="form__items__item">
                    <span class="display">Дата запису</span>
                    <input type="datetime-local" asp-for="RecordDate">
                    <span class="validation" asp-validation-for="RecordDate"></span>
                </div>
                <div class="form__items__item">
                    <input type="submit" value="Редагувати">
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
